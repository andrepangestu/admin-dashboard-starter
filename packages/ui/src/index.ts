/**
 * Promotion target for UI primitives with proven cross-app value (see ADR-006
 * and docs/architecture/design-system.md). App-local components live in
 * `apps/admin/src/shared/ui`; promote one here only once it has multiple real
 * consumers.
 */
export {};
